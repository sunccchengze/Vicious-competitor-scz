# 跟做手册 · 逐行版（S01–S26 全步骤零搜索执行）

> 配套《执行顺序单-每步3小时.md》v2 使用：那份管"为什么和顺序"，本册管"照抄执行"。
> 所有代码均完整给出，无伪码、无"此处省略"。每步末尾：**判据**（怎么算过关）+ **卡壳预案**（一行救命）。
> 标 🔒 的模块必须逐行手敲（能力建设本体），其余可复制粘贴。

---

## 0. 环境准备（一次，30 min，S05 之前完成即可）

**Python 依赖**（本机已有 Anaconda）：
```bash
pip install gmsh numpy pandas matplotlib pytest
```
> `gmsh` 这个 pip 包自带完整网格引擎（Python API），**不需要**另装 Gmsh 桌面版。

**SU2 v8.5.0**（官方发行版，2026-09 实测最新）：
- Windows：下载 `https://github.com/su2code/SU2/releases/download/v8.5.0/SU2-v8.5.0-win64-omp.zip` → 解压到 `C:\SU2` → 把 `C:\SU2\bin` 加入 PATH（系统设置→环境变量→Path→新建）→ 新开终端
- macOS：`SU2-v8.5.0-macos64.zip`，解压后把 bin 目录加进 `~/.zshrc` 的 PATH
- Linux：`SU2-v8.5.0-linux64-omp.zip`，同上
- 选 **omp 版**（单机多线程，免装 MPI）

**判据**：终端输入 `SU2_CFD --help`（Windows 下 `SU2_CFD.exe --help`）出现版本横幅 `v8.5.0`。
**卡壳**：命令找不到 = PATH 没生效，重开终端；仍不行就改用绝对路径 `C:\SU2\bin\SU2_CFD.exe`。

**目录约定**：主战场仓 `delta-eta-min`（S05 创建），下文 `$REPO` 指它的根目录。

---

## S01 · Mr.GUO 整仓私有化 —— ✅ 已完成（本手册开工前）

2026-09-06 双通道验证：网页 404 + API 404。台账记：`S01 ✅ 2026-09-06`。
附带发现：`wendang11` 已是私有（S03 少一项）；`SCZ_Archived` 仍公开（S03 处理）。

---

## S01b · 公开白皮书仓（0.5h）

前提：本地有 Mr.GUO 的克隆（没有就 `git clone https://github.com/sunccchengze/Mr.GUO.git`——现在需要登录才能拉，用你的账号）。

```bash
mkdir guo-whitepaper && cd guo-whitepaper && git init
# 三样东西拷进来（路径按你的本地实际改）
cp /path/to/Mr.GUO/燃气轮机智能设计与前沿算法自学白皮书.md .
cp -r /path/to/Mr.GUO/corpus/facts ./corpus_facts
cp -r /path/to/Mr.GUO/tools ./tools
cp -r /path/to/Mr.GUO/code ./code
printf 'corpus/txt/\n郭老师论文/\n' > .gitignore
```

`README.md`（全文照抄）：
```markdown
# 郭老师论文白皮书 · 公开版
20 篇论文的自学讲义 + 事实卡 + 可运行原型库。
证据等级体系（A/B/C）与〔来源〕标注规范见白皮书著者前言。
论文原件与全文抽取存私有库，本仓仅含笔记、事实卡与代码。
```

```bash
git add -A && git commit -m "init: 白皮书公开版（仅笔记/事实卡/工具）"
gh repo create sunccchengze/guo-whitepaper --public --source . --push
```

**判据**：`git ls-tree -r main --name-only | grep -E "\.pdf$|corpus/txt"` 无输出；浏览器无痕窗口能打开新仓。
**卡壳**：Mr.GUO 私有后本地克隆失效 → `gh repo clone sunccchengze/Mr.GUO`（用你的账号认证）。

---

## S02 · 旗舰仓减重（0.5h）

```bash
cd /path/to/turbine-blade-ai-platform
git rm -r -q --cached "技能库&准则"
printf '技能库&准则/\n' >> .gitignore
# 排查教材目录（命中则同批 --cached 移除）
git ls-tree -r main --name-only | grep -E "^教材/|\.pdf$" | head -20
```

`NOTICES.md` 自动起草（在仓根建 `tools/make_notices.py`，运行一次生成草稿）：
```python
from pathlib import Path
root = Path("技能库&准则")
rows = ["| 目录 | 许可证（自动探测） | 用途（人工填） |", "|---|---|---|"]
for d in sorted(root.iterdir()):
    if not d.is_dir():
        continue
    lic = next((f.name for f in d.iterdir() if "licen" in f.name.lower()), "未找到")
    rows.append(f"| {d.name} | {lic} | （填：在项目里用它做了什么） |")
Path("NOTICES.md").write_text("# 第三方资产清单（迁移存档索引）\n\n" + "\n".join(rows), encoding="utf-8")
print("草稿已生成，逐行人工核对许可证与用途后提交")
```
```bash
python tools/make_notices.py
# 人工核对 NOTICES.md 后：
git add -A && git commit -m "chore: 第三方技能库迁出公开仓，NOTICES 登记来源与用途" && git push
```

**判据**：GitHub 仓库页 Files 根目录看不到 `技能库&准则`；`NOTICES.md` 在。
**卡壳**：历史 blob 仍在 → 不用管（Phase 2 可选 filter-repo），HEAD 干净即可满足爬虫与访客视角。

---

## S03 · 隐私收敛 + 门面（0.3h）

```bash
# SCZ_Archived（21 个单页：情绪/生日/私人内容）转私有
gh repo edit sunccchengze/SCZ_Archived --visibility private --accept-visibility-change-consequences
# 逐仓复查（404=已私有）
for r in SCZ_Archived Mr.GUO wendang11; do curl -s -o /dev/null -w "$r %{http_code}\n" https://github.com/sunccchengze/$r; done
```
门面（网页操作，2 分钟）：GitHub 主页 → Edit profile → Bio 填：
```
XJTU 能动强基 2501 · AI for Turbomachinery
叶轮机械 AI 设计优化：turbine 平台 + 白皮书 20 讲
主线在 pinned 仓库，arena/* 分支为工作区
```
Pin：`turbine-blade-ai-platform` `guo-whitepaper` `wind_farm_viz` `zixue2026`。
**判据**：无痕窗口打开主页，10 秒内能说出你主线。

---

## S04 · 冻结令（0.25h）

仓根新建 `FREEZE-Mainline.md`（全文照抄，方括号处填你的决定）：
```markdown
# 主线冻结令 · 2026-09-06 至 2026-11-06
保留（活跃）：turbine-blade-ai-platform / guo-whitepaper / zixue2026 / delta-eta-min
冻结（只读）：Dachuang-SZLS / wind_farm_viz / Yingzai2026 / [其余全部]
禁开新仓：60 天检查点（2026-11-06）前不新建任何仓库。
scz-Game-Studio 裁决：[做→给截止日 / 删→今天删]
唯一例外：delta-eta-min（S05 建）。
```
```bash
git add -A && git commit -m "docs: 主线冻结令" && git push
```

---

## S05 · delta-eta-min 建仓（0.5h）

```bash
mkdir delta-eta-min && cd delta-eta-min && git init
mkdir -p src tests tools mesh cases runs docs drills
touch src/__init__.py tests/__init__.py
```

`AGENTS.md`（全文）：
```markdown
# 本仓铁律（对任何 AI 会话生效）
1. src/ 下模块必须本人逐行手写，AI 只可审查、不可代写。
2. 核心模块的 commit 由本人邮箱署名，禁止 co-author 署名。
3. AI 允许的范围：tests 夹具、tools/ 脚本、文档装配、图表美化。
4. 每步完成即 commit + 填 LEDGER.md（顺序单台账）。
```

`tests/test_smoke.py`（全文）：
```python
def test_import():
    import src  # noqa: F401

def test_gci_import():
    # S14′ 完成前此测试会失败——这是刻意的"占位红"，逼你完成手写链
    try:
        from src.gci import gci  # noqa: F401
    except ImportError:
        assert False, "src/gci.py 尚未手写完成"
```

```bash
git add -A && git commit -m "init: T1 收益显著性检验框架建仓"
gh repo create sunccchengze/delta-eta-min --public --source . --push
```
**判据**：`pytest` 跑通（test_smoke 里第二条是红的——正常，它就是进度条）。

---

## S07 · 证据源锁定（0.5h，可与 S08 并行）

`docs/SOURCES.md` 建表（数字从私有 Mr.GUO 仓的白皮书 6.2/6.3 节抄录，逐条核页码）：
```markdown
| 代号 | 数字 | 出处（论文/页码/表号） | 在 T1 中的角色 |
|---|---|---|---|
| A1 | 13±1（多次独立运行离散度） | P02 Table 8 / P07 §Performance | "重复统计"参照物 |
| A2 | +1.104% | P07 §（复算回判对象） | 待回判的宣称增益 |
| A3 | 4200 s/样本 | P11 §5.2.1 | 成本口径 |
| A4 | 9 ms vs 0.915 ms（10 倍差） | P16 §4.1.1 / Table 3 | 数值噪声存在的实证 |
```
**判据**：T1 报告将来引用的每个数字都能在此表回溯（沿用白皮书铁律①）。

---

## S08 · SU2 冒烟（1h）

见第 0 节安装。判据即 `SU2_CFD --help` 出横幅。**卡壳**：Windows 报缺 DLL → 装 VC++ Redistributable 2015-2022（微软官网搜 vc_redist.x64.exe）。

---

## S10′ · 🔒 三档网格（1.5h，含 S06 档位定义）

### `mesh/naca0012.geo`（全文逐行；🔒 部分为 1–25 行的翼型数学，建议手敲理解）

```javascript
// NACA0012 · Euler 网格 · lc 由 make_grids.py 注入（LC_TOKEN）
lc = LC_TOKEN;                       // 特征长度（弦长=1）；L1=0.012 L2=0.006 L3=0.003

// ── 翼型型线：闭尾缘 NACA 方程，余弦分布（每边 N 点）──
N = 60;
For i In {1 : N}
  x = 0.5*(1 - Cos(Pi*i/N));         // 余弦加密：前后缘密
  y = 0.6*(0.2969*Sqrt(x) - 0.126*x - 0.3516*x^2 + 0.2843*x^3 - 0.1036*x^4);
  pu[i] = newp; Point(pu[i]) = { x,  y, 0, lc};
  pl[i] = newp; Point(pl[i]) = { x, -y, 0, lc};
EndFor
pLE = newp; Point(pLE) = {0, 0, 0, lc};
pTE = newp; Point(pTE) = {1, 0, 0, lc};

upp[] = {pLE};
For i In {1 : N} upp[] += pu[i]; EndFor
upp[] += pTE;                         // 上表面 LE→TE
low[] = {pTE};
For i In {1 : N} low[] += pl[N+1-i]; EndFor
low[] += pLE;                         // 下表面 TE→LE（闭环）

Spline(1) = upp[];
Spline(2) = low[];
Curve Loop(1) = {1, 2};               // 翼型 = 洞

// ── 远场方框 20c×20c ──
D = 10;
p1 = newp; Point(p1) = {-D, -D, 0, 10*lc};
p2 = newp; Point(p2) = { D, -D, 0, 10*lc};
p3 = newp; Point(p3) = { D,  D, 0, 10*lc};
p4 = newp; Point(p4) = {-D,  D, 0, 10*lc};
Line(3) = {p1, p2}; Line(4) = {p2, p3}; Line(5) = {p3, p4}; Line(6) = {p4, p1};
Curve Loop(2) = {3, 4, 5, 6};

Plane Surface(1) = {2, 1};            // 外环 − 内洞 = 计算域

// ── 尺寸场：近翼型 lc，随距离线性放大（控总量）──
Field[1] = Distance;  Field[1].CurvesList = {1, 2}; Field[1].NNodesByEdge = 100;
Field[2] = MathEval;  Field[2].F = Sprintf("%g + 0.1*F1", lc);
Field[3] = Min;       Field[3].FieldsList = {2};
Background Field = 3;

// ── 物理组：名字必须与 SU2 cfg 的 MARKER_* 逐字一致 ──
Physical Curve("Airfoil")  = {1, 2};
Physical Curve("Farfield") = {3, 4, 5, 6};
Physical Surface("Domain") = {1};
```

### `tools/make_grids.py`（全文；属工具类，可复制）

```python
"""S10′: 三档网格生成。用法: python tools/make_grids.py"""
from pathlib import Path
import gmsh

LEVELS = {"L1": 0.012, "L2": 0.006, "L3": 0.003}   # 细化比 r=2（≥1.3 达标）
ROOT = Path(__file__).resolve().parents[1]
tpl = (ROOT / "mesh" / "naca0012.geo").read_text(encoding="utf-8")

for name, lc in LEVELS.items():
    tmp = ROOT / "mesh" / f"_{name}.geo"
    tmp.write_text(tpl.replace("LC_TOKEN", repr(lc)), encoding="utf-8")
    gmsh.initialize()
    gmsh.option.setNumber("General.Terminal", 1)
    gmsh.open(str(tmp))
    gmsh.model.mesh.generate(2)
    out = ROOT / "mesh" / f"mesh_{name}.su2"
    gmsh.write(str(out))
    tags = gmsh.model.getPhysicalGroups(1)
    marks = [gmsh.model.getPhysicalName(d, t) for d, t in tags]
    n2 = sum(len(e[0]) for e in gmsh.model.mesh.getElements(2))
    print(f"[{name}] lc={lc} 单元数={n2} markers={marks} -> {out.name}")
    gmsh.finalize()
    tmp.unlink()
```

```bash
python tools/make_grids.py
```
**判据**：三行输出，L1≈几千、L3≈一二十万级单元；markers 含 `Airfoil` 与 `Farfield`。
**卡壳**：① L3 太慢 → L3 改 0.004（r=1.5 仍达标）；② markers 缺失 → 检查 .geo 最后三行引号拼写；③ gmsh 报 CurvesList → 老版本改回 `EdgesList`。

---

## S12′ · 配置 + 批量跑（1h 监控 + 挂机）

### `cases/naca0012.cfg`（全文，一阶基础版；对照 SU2 v8.5 模板逐键理解）

```
% ---- 求解器与网格 ----
SOLVER= EULER
MESH_FILENAME= mesh_L1.su2
MESH_FORMAT= SU2
% ---- 来流（AGARD AR-303 经典跨声速工况）----
MACH_NUMBER= 0.8
AOA= 1.25
SIDESLIP_ANGLE= 0.0
INIT_OPTION= TD_CONDITIONS
FREESTREAM_OPTION= TEMPERATURE_FS
FREESTREAM_TEMPERATURE= 288.15
FREESTREAM_PRESSURE= 101325.0
% ---- 力系数归一（弦长=1）----
REF_AREA= 1.0
% ---- 边界（与 .geo 物理组逐字一致）----
MARKER_EULER= ( Airfoil )
MARKER_FAR= ( Farfield )
% ---- 数值格式：一阶 ----
NUM_METHOD_GRAD= GREEN_GAUSS
CONV_NUM_METHOD= ROE
MUSCL_FLOW= NO
TIME_DISCRE_FLOW= EULER_IMPLICIT
CFL_NUMBER= 4.0
LINEAR_SOLVER= FGMRES
LINEAR_SOLVER_PREC= ILU
LINEAR_SOLVER_ERROR= 1e-6
LINEAR_SOLVER_ITER= 20
% ---- 步数与收敛判据：残差降 10 个量级或 1000 步 ----
ITER= 1000
CONV_FIELD= RESIDUAL
CONV_RESIDUEVAL= 10
CONV_STARTITER= 10
% ---- 输出：history.csv 含残差与升阻力系数 ----
HISTORY_OUTPUT= (ITER, RMS_RES, AERO_COEFF)
```

### `tools/run_levels.py`（全文）

```python
"""S12′: 每档独立目录跑 SU2。用法: python tools/run_levels.py L1 L2 L3"""
import subprocess, shutil, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SU2 = shutil.which("SU2_CFD") or shutil.which("SU2_CFD.exe")
assert SU2, "找不到 SU2_CFD：先完成 S08 并加入 PATH"

for lvl in sys.argv[1:]:
    d = ROOT / "runs" / lvl
    d.mkdir(parents=True, exist_ok=True)
    cfg = (ROOT / "cases" / "naca0012.cfg").read_text(encoding="utf-8")
    cfg = cfg.replace("mesh_L1.su2", f"../../mesh/mesh_{lvl}.su2")
    (d / "config.cfg").write_text(cfg, encoding="utf-8")
    print(f"[{lvl}] 运行中 -> {d}")
    with open(d / "su2.log", "w", encoding="utf-8") as log:
        subprocess.run([SU2, "config.cfg"], cwd=d, stdout=log,
                       stderr=subprocess.STDOUT, check=True)
    print(f"[{lvl}] 完成，收敛史: {d/'history.csv'}")
```

```bash
python tools/run_levels.py L1 L2 L3
```
挂机时填 `runs/README.md` 台账：档位 / 网格单元数 / 步数 / 残差降幅。
**判据**：三个 `runs/L*/history.csv` 存在；su2.log 尾部无 divergence 字样。
**卡壳**：残差 100 步内爆掉 → cfg 里 `CFL_NUMBER= 4.0` 改 `1.0` 重跑。

---

## S13′ · 🔒 收敛史解析器（2h，手敲）

`src/convergence.py`（规格：输入 history.csv → 输出摘要 dict；平台期定义=迭代末 25% 段）：

```python
"""S13′ 收敛史解析器。手写模块：SU2 history.csv -> 收敛摘要。"""
from __future__ import annotations
import numpy as np
import pandas as pd


def load_history(path):
    df = pd.read_csv(path)
    df.columns = [c.strip().strip('"') for c in df.columns]

    def pick(*cands):
        for c in cands:
            for col in df.columns:
                if col.strip('"').lower() == c.lower():
                    return col
        raise KeyError(f"缺列 {cands}；实际列：{list(df.columns)}")

    return (df,
            pick("Inner_Iter", "ITER", "Outer_Iter"),
            pick("rms[Rho]"),
            pick("CD"),
            pick("CL"))


def orders_dropped(rms):
    x = np.asarray(rms, float)
    return float(np.log10(x[0]) - np.log10(np.nanmin(x)))


def plateau_stats(values, tail_frac=0.25, min_tail=50):
    x = np.asarray(values, float)
    x = x[np.isfinite(x)]
    k = max(min_tail, int(len(x) * tail_frac))
    t = x[-k:]
    mean = float(t.mean())
    std = float(t.std(ddof=1))
    return dict(n=k, mean=mean, std=std,
                rel_std=std / abs(mean) if mean else float("inf"),
                samples=t.tolist())


def summary(path):
    df, it, rms, cd, cl = load_history(path)
    return dict(file=str(path),
                iters=int(df[it].iloc[-1]),
                orders_drop=orders_dropped(df[rms]),
                CD_mean=plateau_stats(df[cd])["mean"],
                CL_mean=plateau_stats(df[cl])["mean"],
                CD_plateau=plateau_stats(df[cd]))
```

`tests/test_convergence.py`（全文，fixture 造假日志）：
```python
import numpy as np
from src.convergence import summary, plateau_stats


def _fake_history(tmp_path, cd_drift=1e-5):
    import pandas as pd
    n = 200
    rows = {"Inner_Iter": np.arange(n),
            "rms[Rho]": 1e-1 * 10 ** (-np.arange(n) / 20),
            "CD": 0.0227 + cd_drift * np.random.default_rng(0).standard_normal(n),
            "CL": 0.35 + 0.1 * np.exp(-np.arange(n) / 30)}
    p = tmp_path / "history.csv"
    pd.DataFrame(rows).to_csv(p, index=False)
    return p


def test_summary(tmp_path):
    s = summary(_fake_history(tmp_path))
    assert s["iters"] == 199
    assert s["orders_drop"] > 9            # 假残差降了 ~10 个量级
    assert abs(s["CD_mean"] - 0.0227) < 1e-4


def test_plateau_rel_std_small(tmp_path):
    s = summary(_fake_history(tmp_path))
    assert s["CD_plateau"]["rel_std"] < 0.01
```
**判据**：`pytest tests/test_convergence.py` 绿；commit 无 co-author。

---

## S14′ · 🔒 GCI 模块（2h，手敲）

`src/gci.py` 规格：等比三档（本项目 r=2）→ 观察阶数 p、Richardson 外推值、GCI%。公式源：Roache / Celik et al. (2008) 标准。

```python
"""S14′ 网格收敛指数（等比三档，Roache/Celik 2008 标准）。手写模块。"""
from dataclasses import dataclass


@dataclass
class GCIResult:
    p_obs: float          # 观察阶数（与理论阶对照：一阶≈1，二阶≈2）
    phi_ex: float         # Richardson 外推"连续解"
    gci_fine_pct: float   # 细网格上的不确定度（Fs=1.25，95% 置信）
    gci_med_pct: float
    e21: float
    e32: float


def gci(phi_fine, phi_med, phi_coarse, r=2.0, Fs=1.25):
    if abs(r) <= 1.0:
        raise ValueError("细化比 r 必须 > 1")
    e21 = phi_med - phi_fine
    e32 = phi_coarse - phi_med
    if e21 == 0 or e32 == 0 or ((e21 > 0) != (e32 > 0)):
        raise ValueError(
            f"不在单调渐近收敛区：e21={e21:g}, e32={e32:g}；"
            "对策=再加细一档，或检查三档是否同一收敛口径")
    p = __import__("math").log(abs(e32 / e21)) / __import__("math").log(r)
    phi_ex = phi_fine + (phi_fine - phi_med) / (r ** p - 1)
    g21 = Fs * abs(e21 / phi_fine) / (r ** p - 1) * 100 if phi_fine else float("inf")
    g32 = Fs * abs(e32 / phi_med) / (r ** p - 1) * 100 if phi_med else float("inf")
    return GCIResult(p, phi_ex, g21, g32, e21, e32)
```
（手敲时把 `__import__("math")` 换成顶部正常 `import math`——这里是为了行内自包含。）

`tests/test_gci.py`（解析解自检：造一阶精确解，看能否复原）：
```python
from src.gci import gci


def test_first_order_recovery():
    # phi(h) = 1 + C*h，C=1，h=1/0.5/0.25（r=2）→ 真值 1
    res = gci(1.25, 1.5, 2.0, r=2)
    assert abs(res.p_obs - 1.0) < 1e-9     # 复原一阶
    assert abs(res.phi_ex - 1.0) < 1e-9    # Richardson 精确复原真值
    assert res.gci_fine_pct > 0


def test_second_order_recovery():
    res = gci(1.0625, 1.25, 1.5, r=2)      # phi = 1 + h^2
    assert abs(res.p_obs - 2.0) < 1e-9
    assert abs(res.phi_ex - 1.0) < 1e-9


def test_oscillatory_rejected():
    import pytest
    with pytest.raises(ValueError):
        gci(1.0, 1.1, 1.05, r=2)           # 非单调差 → 拒出
```
**判据**：`pytest tests/test_gci.py` 绿（两个解析解精确复原 = 你的公式对）。

---

## S15′ · 🔒 Δ_min 显著性模块（2.5h，★全计划心脏）

`src/significance.py` 规格：Δ_min = max(GCI 绝对量, 平台期 bootstrap 95%CI 半宽)；判据函数 `is_significant`。

```python
"""S15′ 最小可分辨增量框架（bootstrap 口径）。手写模块。
宣称增益 > Δ_min 才可称"成果"——白皮书 6.2① 的可执行化。"""
import numpy as np
from src.gci import gci


def bootstrap_ci(samples, B=10000, alpha=0.05, seed=42):
    """平台期样本 -> (CI下界, CI上界, 半宽)。均值统计量。"""
    rng = np.random.default_rng(seed)
    x = np.asarray(samples, float)
    n = x.size
    boots = np.empty(B)
    for b in range(B):
        boots[b] = rng.choice(x, n, replace=True).mean()
    lo, hi = np.quantile(boots, [alpha / 2, 1 - alpha / 2])
    return float(lo), float(hi), float((hi - lo) / 2)


def delta_min(plateau_samples_fine, phi_fine, phi_med, phi_coarse,
              r=2.0, B=10000):
    """合成最小可分辨增量：网格不确定度与统计不确定度取大者。"""
    _, _, half = bootstrap_ci(plateau_samples_fine, B=B)
    g = gci(phi_fine, phi_med, phi_coarse, r=r)
    gci_abs = g.gci_fine_pct / 100.0 * abs(phi_fine)
    return dict(boot_half=half,
                gci_abs=gci_abs,
                dmin=max(half, gci_abs),
                p_obs=g.p_obs,
                phi_ex=g.phi_ex)


def is_significant(claimed_delta, dmin):
    """返回 (是否显著, 边际)。claimed_delta 用绝对值口径传入。"""
    margin = abs(claimed_delta) - dmin
    return margin > 0, dict(margin=margin,
                            verdict="显著" if margin > 0 else "不可分辨")
```

`tests/test_significance.py`：
```python
import numpy as np
from src.significance import bootstrap_ci, delta_min, is_significant


def test_bootstrap_covers_truth():
    x = np.random.default_rng(1).normal(0.5, 0.01, 400)
    lo, hi, half = bootstrap_ci(x)
    assert lo < 0.5 < hi
    assert 0 < half < 0.01


def test_delta_min_combines():
    x = np.random.default_rng(2).normal(1.25, 1e-4, 300)   # 平台期噪声极小
    d = delta_min(x, phi_fine=1.25, phi_med=1.5, phi_coarse=2.0, r=2)
    assert d["dmin"] >= d["boot_half"] and d["dmin"] >= d["gci_abs"]


def test_verdicts():
    ok, v1 = is_significant(0.10, 0.01)
    no, v2 = is_significant(0.005, 0.01)
    assert ok and v1["verdict"] == "显著"
    assert (not no) and v2["verdict"] == "不可分辨"
```
**判据**：`pytest` 全绿；commit 无 co-author。**到这一步，你的手写证据链（S13′+S14′+S15′）成型。**

---

## S16′ · 二阶深收敛（0.5h 监控 + 挂机）

`cases/naca0012_2nd.cfg`：复制 `naca0012.cfg`，只改 5 行：
```
MUSCL_FLOW= YES
SLOPE_LIMITER_FLOW= VENKATAKISHNAN
VENKAT_LIMITER_COEFF= 0.05
CFL_NUMBER= 1.5
ITER= 3000
```
```bash
# 用 run_levels.py 同款方式跑 L2、L3（改脚本里 cfg 文件名，或临时替换 cases/naca0012.cfg 内容）
```
**判据**：`history.csv` 残差降 ≥4 个量级（Euler 二阶带限制器常见平台在 1e-8，达标）。跑成 → LEDGER 标 🏁 **第一个个人 E4**。
**卡壳**：二阶震荡不收敛 → `VENKAT_LIMITER_COEFF= 0.02` 收紧限制器。

---

## S17′ · 两张图（1h，🤖 出图你核数）

`tools/plot_results.py`（全文）：
```python
"""S17′: fig1 网格收敛 + GCI 带；fig2 宣称增益 vs Δ_min 判据带。"""
import json
from pathlib import Path
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
import numpy as np
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from src.convergence import summary
from src.significance import delta_min

ROOT = Path(__file__).resolve().parents[1]
RUNS = {L: summary(ROOT / "runs" / L / "history.csv") for L in ("L1", "L2", "L3")}
H = {"L1": 0.012, "L2": 0.006, "L3": 0.003}          # h: 粗->细
Lv = ["L1", "L2", "L3"]
phi = {L: RUNS[L]["CD_mean"] for L in Lv}

d = delta_min(RUNS["L3"]["CD_plateau"]["samples"],
              phi["L3"], phi["L2"], phi["L1"])
out = ROOT / "docs"
out.mkdir(exist_ok=True)
(out / "delta_min.json").write_text(json.dumps(d, indent=2), encoding="utf-8")

plt.rcParams.update({"font.size": 9, "axes.linewidth": 0.6})
# fig1：phi vs h + 外推点
fig, ax = plt.subplots(figsize=(4.2, 3.2), dpi=300)
ax.plot([H[L] for L in Lv], [phi[L] for L in Lv], "o-", ms=4, lw=0.8, label="CD(h)")
ax.axhline(d["phi_ex"], lw=0.8, ls="--", label=f"Richardson $\\phi_{{ex}}$={d['phi_ex']:.5f}")
ax.axhspan(d["phi_ex"] - d["dmin"], d["phi_ex"] + d["dmin"], alpha=0.12,
           label=f"$\\Delta_{{min}}$={d['dmin']:.2e} (p={d['p_obs']:.2f})")
ax.set_xlabel("mesh size h"); ax.set_ylabel("CD"); ax.legend(frameon=False)
fig.savefig(out / "fig1_gci.png", bbox_inches="tight"); plt.close(fig)

# fig2：宣称增益 vs 判据带（示例：CD 尺度）
CD0 = phi["L3"]
fig, ax = plt.subplots(figsize=(4.2, 3.2), dpi=300)
claims = [("本项目 L1→L3 网格差", abs(phi["L1"] - phi["L3"])),
          ("L2→L3 网格差", abs(phi["L2"] - phi["L3"]))]
y = np.arange(len(claims))
ax.barh(y, [c[1] for c in claims], height=0.45, lw=0)
ax.axvline(d["dmin"], color="crimson", lw=1.0)
ax.text(d["dmin"], len(claims) - 0.4, " Δ_min（此线左侧=不可分辨）", color="crimson", fontsize=8)
ax.set_yticks(y, [c[0] for c in claims]); ax.set_xlabel("|Δ CD|")
fig.savefig(out / "fig2_deltamin.png", bbox_inches="tight"); plt.close(fig)
print("figs ->", out / "fig1_gci.png", out / "fig2_deltamin.png", "| dmin =", d["dmin"])
```
**判据**：两图生成，数字与 JSON 一致。fig2 就是 T1 报告的核心论据雏形——60 天后把 P07 的 +1.104% 换算进来回判。

---

## S18 · 白板推导（45min × 12，题目表见顺序单 v2）

操作规格：手机横拍 → 存 `drills/WW01_nsga2.mp4` + 同名 `jpg`；讲卡壳的点记 `BLOCKERS.md`。

## S19′ · 给郭老师的信（0.75h，模板全文）

> 郭老师：
> 您好，我是能动强基 2501 的孙承泽。上次去信后我把您 20 篇论文整理成了一份带证据等级的自学讲义（附件 PDF，超出摘要的内容均标注"原文未获取"，未虚构）。
> 整理中我复算了一个跨声速算例的三档网格，发现 [S12′-S14′ 的真实数字，如"网格间的 CD 差与迭代平台期噪声同量级"]。想请教您：课题里报效率增益时，最小可分辨增量您一般怎么定？
> 如果方便，希望能旁听一次组会，或请师兄师姐看一眼我的复现脚本（附件仓库链接）。
> 孙承泽

写完冷置一夜，次晨只改错别字就发。禁止第二轮融资式打磨。

## S20 · 反馈邮件（模板）
> 师兄/师姐好，我是准备进组的孙承泽。附件是我做的网格无关性 + 增益显著性小框架（两张图 + 一段代码）。想请您给三条最狠的批评，直接说哪里不对就行。

## S21′ · 赛事默认决策（0.25h）
两周内无更近截止日 → 默认**美赛（明年 2 月）**，写入 FREEZE-Mainline.md 附录，结案不再议。

## S22′ · E4 报告（1h，骨架全文）
`docs/E4_REPORT.md`：
```markdown
# E4 收敛报告：NACA0012 Euler 跨声速（M=0.8, α=1.25°）
## 1 设置（SU2 v8.5.0 / cfg 全文链接 runs/L*/config.cfg）
## 2 三档网格表（单元数/h 比/r）
## 3 收敛史（orders_drop ≥ 4 证据截图）
## 4 GCI 结果表（p_obs / φ_ex / GCI%，一阶 vs 二阶对照）
## 5 局限（Euler 无粘/等比网格/单一观测量 CD）
```
## S23′ · 线上站挂 E4（0.75h，🤖 全包）
把 fig1/fig2 + E4_REPORT 摘要挂 turbine 平台 methodology 页新卡片，证据等级标 E4，部署绿即收。

## S24′ · T1 报告 v1（1.5h，骨架全文）
`docs/T1_REPORT.md`：动机（引白皮书 6.2①/ SOURCES.md A4）→ 方法定义（Δ_min 三源合成，引 S15′ 模块）→ 复算实证（fig1/fig2）→ 回判表（P07 的 +1.104% vs ΔCD_min）→ 局限。自检三问：训练集还是测试集？怎么复现？哪个数字没出处？

## S25 · 师兄评审（1h）：要 3 条狠批 → `docs/review_r1.md` 逐条落实或反驳。

## S26 · 60 天对表（1h，🔒）
```bash
git log --oneline --author="2253710052" --since="2026-09-06" | wc -l   # 手写 commit 数
ls drills/ | wc -l                                                     # 白板次数
```
七项检查点逐项 git 取证，诚实写 `docs/AUDIT-1106.md`，未完成不许擦除。

---

## 全册依赖一览

```
S01✅ → S01b → S02 → S03 → S04 →（S05+环境准备）→ S07 ∥ S08 → S10′ → S12′
→ S13′ → S14′ → S15′ → S16′ → S17′ → S19′ → S22′/S23′/S24′ → S25 → S26
S18 全程每周固定槽
```
