# 模块化执行顺序单 v2 · 快进版（每步 ≤3 小时）

> v2 · 2026-09-06 修订：按"能简则简"原则全面提速。**总工时约砍半（≈40h → ≈21h），关键链（手写 + 真跑 + 真人）一分钟没砍。**
> 配套：《行动计划-孙承泽-2026秋冬.md》（战略层）
>
> ### v2 简化总判据（先记住这条再做事）
> **一步的价值在"做"还是在"有"？**
> - 在**"有"**的（脚手架、README、图表、报告排版、门面、政策文档）→ 全部 AI 生成 + 人工核数字，分钟级；
> - 在**"做"**的（手写统计模块、真跑求解器、白板推导、真人对话）→ **不可简化**，简化 = 删掉战略本身（弱点 No.1/No.9 原样复发）。
>
> ### v2 三处结构性简化（比逐步抠时间更狠）
> 1. **算例降维**：v1 用 3D Rotor 37（从零画网格，新手高风险，云租机 ¥300）→ **v2 改用 2D/半3D 教程级跨声速算例（默认 ONERA M6，NACA0012 备选）**。T1 的贡献是 Δη_min **框架**，不是叶栅几何；先在便宜算例上把方法论闭环，叶栅升级版列为 60 天后的可选 Phase 2。**云端租机整体推迟到 Phase 2**（2D 算例笔记本整夜能跑，¥0）。
> 2. **合并砍步**：S06 并入 S10′（一个脚本出三档网格）；S09 整步砍掉（教程跑通即冒烟，"手感"从 GCI 复跑里长出来）；S05/S07 压缩为 AI 起草 + 人工核对。
> 3. **单口径原则**：S15 统计检验 v1 只做 bootstrap 一种口径（t 检验留 v2 报告再补）。
>
> ### 不可简化清单（🔒 保护名单，谁提都不砍）
> `S13′ 解析器 / S14′ GCI / S15′ Δη_min`（手写核心）→ `S16′ 二阶真跑` → `S18 白板 ×12` → `S19/S20 真人信与反馈` → `S26 对表`。其余全部可提速。
>
> ### 铁律（v1 三条不变）
> 3 小时做不完 = 步骤设计错误，当场拆两步；卡壳 45 分钟 = 写 `BLOCKERS.md` 跳步；收尾必 commit + 填台账。挂机等待不计入 3h。

---

## 进度总台账（复制为仓库根 `LEDGER.md`，随做随填）

| 步骤 | 名称 | v1预算 | **v2预算** | 状态 | commit | 完成日 |
|---|---|---|---|---|---|---|
| S01 | Mr.GUO 整仓私有化 | 0.1h | **🟢 决定撤销** | 09-07 本人决定保持公开（知情接受风险，监督协议监控）；09-09 复查确认 public | — | — |
| S01b | 公开白皮书仓（极简版） | 2h | **0.5h** 🤖 | 已取消（09-06 本人决定，白皮书保持私有，出口=S19′ 信件 PDF 附件） | — | 09-06 |
| S02 | 旗舰仓减重+NOTICES | 1h | **0.5h** 🤖 | ✅ 09-07 闭环（main=624 文件，-98.6%；NOTICES 72 条；保留白名单带许可） | f93801ab | 09-07 |
| S03 | 隐私收敛+门面 | 1h | **0.3h** | ✅ 09-07 政策结案（wendang11 已删；bio/pin 按需自定；SCZ_Archived 保留公开为本人选择） | — | 09-07 |
| S04 | 冻结令 | 0.5h | **0.25h** 🤖 | ✅ 09-07 成文后**降级为监督协议**（本人决定；FREEZE 文件留作参考清单） | 38e1abb2 | 09-07 |
| S05 | delta-eta-min 建仓 | 2h | **0.5h** 🤖 | ✅ 09-07 超额闭环（骨架+SPEC 三份+路径B numpy 网格已实测 2304/9216/36864+占位红进度条+署名红线；沙箱无 SU2 部分如实标注待本机） | 73611ab | 09-07 |
| ~~S06~~ | ~~数据/档位模块~~ | 3h | **并入 S10′** | — | | |
| S07 | T1 证据源锁定 | 2h | **0.5h** 🤖 | ✅ 09-09（本人报：SOURCES.md 数字→页码→用在哪，逐行核完） | — | 09-09 |
| S08 | 本地装 SU2+冒烟 | 3h | **1h** | ✅ 09-09 随 §0 环境准备完成（SU2 v8.5.0 安装+冒烟横幅，本人报） | — | 09-09 |
| ~~S09~~ | ~~官方案例全跑通~~ | 3h | **砍掉**（并入 S08/S12′） | — | | |
| S10′ | 🔒 一个 .geo 脚本出三档网格 | 6h(S10+11) | **1.5h** | ✅ 09-09 本机三档跑通（L3=2,961,434、markers 齐；与沙箱差 2 单元=平台三角化差异；100 步启动验并入 S12′ 首启） | — | 09-09 |
| S12′ | 三档一阶批量跑（本地） | 2h | **1h** 🔧→✅ | L1 实跑通过（09-10：1000 行、CD=0.0286、CL=0.2455 稳定；勘误#9 生效）；L2/L3 已跑完，待本人回传三档末段值销项 | | |
| S13′ | 🔒 收敛史解析器 | 3h | **2h** | ☐ | | |
| S14′ | 🔒 GCI 模块 | 3h | **2h** | ☐ | | |
| S15′ | 🔒 Δη_min 检验（仅 bootstrap） | 3h | **2.5h** | ☐ | | |
| S16′ | ⏳ 细档二阶深收敛（本地整夜） | 2h | **0.5h** ⏳ | ☐ | | |
| S17′ | 两张图（AI 出图你核对） | 3h | **1h** 🤖 | ☐ | | |
| S18 | 白板推导 ×12 | 0.75h/次 | **不变**（降摩擦） | ☐ | | |
| S19′ | 给郭老师第二封信 | 2h | **0.75h** | ☐ | — | |
| S20 | 真人反馈 ×3 | 1h/次 | **不变** | ☐ | — | |
| S21′ | 赛事默认决策 | 1h | **0.25h** | ✅ 默认美赛（写入 FREEZE 附录 A） | 38e1abb2 | 09-07 |
| S22′ | E4 报告（AI 装配+核数） | 3h | **1h** 🤖 | ☐ | | |
| S23′ | 线上站新页（AI 全包） | 3h | **0.75h** 🤖 | 🔶 半步 ✅（占位骨架已上 arena 分支：E4 卡片+两 FIG 槽位，「复算完成前不展示任何数值」纪律满分；数据待 S17′） | 38e1abb2 | 09-07 |
| S24′ | T1 报告 v1（AI 装配+核数） | 3h | **1.5h** 🤖 | ☐ | | |
| S25 | 师兄评审一轮 | 1h | **1h** | ☐ | | |
| S26 | 60 天对表审计 | 1h | **1h** 🔒 | ☐ | | |

**核心链工时：v1 ≈ 40h → v2 ≈ 21h**（不含 S18/S20 固定槽）。**60 天检查点不变：2026-11-06。**

### 📌 监督协议（2026-09-07 本人决定，替代 FREEZE 禁令）
**事前不禁、事后提醒。** 不再限制开仓/冻结旧仓；本人自主管理注意力。提醒触发线仅三类：
① 版权/法律风险出现实际信号（如出版商通知、DMCA）；② 科研诚实红线（如宣称未经复算的结论、src/ 出现代写痕迹）；③ 主线漂移连续超过两周。
已按此协议销案：Summer-Tutor-07（原创支教课件存档，豁免）、scz-Game-Studio（不再要求处理）、FREEZE-Mainline.md 中冻结表降级为参考清单。

### 09-07 复核纪要（Arena 会话 01a07a16 进度验收 · 已结/待办）
1. ✅ **S02 闭环**：main = f93801ab，44,728→624 文件（-98.6%），保留带 MIT 许可的两份写作规则 + 捞回 deep-tutor——裁量成熟。
2. 🟢 **Mr.GUO 保持公开**：本人知情决定（agent 取用方便；接受出版商爬虫/DMCA 风险）。监控条款：若收到出版商任何通知，立即提醒转私有。此决定不再重提。
3. ✅ **wendang11 已删除**（本人确认）。
4. 🟡 **guo-whitepaper 空公开仓**：仍待删（10 秒小事，提醒一次即止）。~~→ 09-09 结案，见下~~
5. 🔶 **delta-eta-min 空壳**：建仓于 09-07 09:12，0 提交 0 文件——S05 未完成，骨架命令已备（见手册）。~~→ 09-07 当日 S05 ✅ @73611ab 闭环，见台账 S05 行~~

### 09-09 进度更新（Arena 会话 01a085fd · 本仓同步 + 09-07 待办销项）
1. ✅ **main 合入（"main 未合"待办结案）**：本仓 main 由 c78da01 快进至 54d8ce2（`git push origin <分支>:main`，0 PR、线性历史），与 arena/01a085fd 同指。
2. ✅ **空仓待删结案**：guo-whitepaper 已不存在——该路径现指向 delta-eta-min（空仓改名复用为主战场仓），不再有独立空公开仓。
3. ✅ **Mr.GUO 复公开复查**：09-09 API 确认 visibility=public，与 09-07 决定一致；监控条款不变（出版商任何通知即转私有）。
4. ℹ️ **delta-eta-min 现状**：main=f0ae192（09-07），S05 骨架+SPEC+台账在位（其 PR#1/#2 已合），下一棒 S07/S10′。
5. ℹ️ **本仓分支卫生**：旧会话分支 arena/01a076c1 已删（内容全含于现分支，0 提交丢失、无 PR 引用）；现仅 main + arena/01a085fd。
6. 🔬 **指导文件沙箱审计（手册代码原样抽出实跑，09-09）**：
   - ✅ pytest 10/10 全绿（src/{convergence,gci,significance}.py + 4 测试）；plot_results.py 实跑通过（fig1/fig2/delta_min.json 出件，演示 history 下 dmin=8.5e-04、p_obs=0.94）。
   - 🔴 **真 bug·几何（最严重）**：`naca0012.geo` 用 `Spline`（NURBS）过 61 个密集点 → gmsh 4.15 下边界偏离 NACA 至 **0.0565**（理论最大半厚 0.0599），翼型塌成 0.0046 薄片+前缘鼓包到 x=−0.06。最小复现（仅点+Spline+Mesh 1）实锤是 spline 振荡，非点坐标问题（点本身机器精度正确）。**勘误#5 已并入手册正文**：改 60 段/边 Line 链（gmsh 4.15 不支持 `..` 范围，用 For 循环数组累积线 tag）；修后实测边界最大偏差 **6.5e-05 (L1) / 1.7e-04 (L2) / 3.8e-04 (L3)**，闭环、x∈[0,1]、最大半厚 0.06@0.297。附带实测：`Physical Curve("Airfoil")` 必须直接引用线数组——写 `{1,2}` loop tag 不落到线段元素，.su2 会丢 Airfoil marker。
   - 🟠 **真 bug·代码**：`make_grids.py` 数单元 `len(e[0])` 在 gmsh 4.x Python API 崩（**勘误#6**：改读 .su2 的 `NELEM=` 行；.su2 头也没有 `# 2D ELEMENTS` 注释行）。
   - 🟠 **判据数量级错 ~30 倍（勘误#7）**：三档实测 **L1=205,416 / L2=770,224 / L3=2,961,432** 单元（手册原写「L1≈几千、L3≈一二十万」）；细化比每档 ×≈3.8；三档生成 2.5 分钟（L3 三百万格，SU2 求解时太慢就落回 L3=0.004 卡壳方案）。
   - ✅ **环境指令核实**：SU2 v8.5.0 = 官方 Latest（2026-04-27），win64-omp 下载命名成立（changelog 确认 v8.5 只发 -omp 二进制）；PyPI **无 `su2` 包**（pypi.org/project/su2 = 404，`su2gui` 只是 GUI 壳）——SU2 本体只能走 GitHub Releases 二进制，手册第 0 节写法正确，无需改。
   - ℹ️ 未审计项：delta-eta-min 仓库文件（本人划线取消）。
7. ✅ **§0 环境准备完成（09-09 本人报）**：Python t1 环境 + SU2 v8.5.0 安装/PATH/冒烟横幅全部就绪；S08 判据即 §0 冒烟横幅，随之一并结案。下一站 S07 → S10′（按勘误#5–#7 修正后的正文敲）。
8. ✅ **S10′ 三档网格本机跑通（09-09 本人跑）**：`make_grids.py` 三档全出，L3=2,961,434 单元（沙箱 2,961,432，差 2=平台三角化差异）、markers=['Airfoil','Farfield'] 齐、L3 网格耗时 102s（0 错误 1 警告，警告非阻塞）；「SU2 各能启动 100 步」验并入 S12′ 首启。
9. ✅ **S07 证据源锁定完成（09-09 本人报）**：`docs/SOURCES.md` 成表，数字→页码→用在哪逐行核完。阶段 A 全部结案。
10. 🔧 **S12′ L1 首启 exit 1（09-09 排查）**：`run_levels.py` 调 SU2_CFD.EXE 非零退出。已按 SU2 v8.5 官方网格读取器源码（CSU2ASCIIMeshReaderBase/FVM）逐行核：gmsh 写的 .su2（节点行尾带 id、单元行尾带 elem_id、0 起编号、NELEM 先于 NPOIN）v8.5 的 reader 全部兼容（多余 token 被 istringstream 忽略、NMARK 在末即可）——**网格格式排除**。
11. 🎯 **S12′ 根因锁定 + 手册 cfg 勘误#8（09-09）**：本人回传 su2.log → `Line 29 SPECIFIC_HEAT_RATIO`、`Line 59 OUTPUT_FORMAT` invalid option name（v6/v7 老键，v8 为 `GAMMA_VALUE`/`OUTPUT_FILES`）→ **本机 `cases\naca0012.cfg` 不是手册版**（59+ 行旧模板版）。顺手把手册 cfg 对 v8.5.0 官方模板（14 段全拉）+ 官方 QuickStart 逐键核验：25/27 键通过，**3 键系 v6 遗留**——`CONV_NUM_METHOD`（v8.5 改名 `CONV_NUM_METHOD_FLOW`）、`CONV_RESIDUEVAL`（v8.5 模板无此键、全仓 0 命中，改 `CONV_RESIDUAL_MINVAL= -10` log10 目标值）、`CONV_FIELD= RESIDUAL`（官方 QuickStart/TestCases 一律用具名字段，改 `RMS_DENSITY`）——手册已修。**另核验 S13′ 解析器与 v8.5 真实 history.csv 列名（`Inner_Iter`/`rms[Rho]`/`CD`/`CL`，源码 `AddHistoryOutput` 定案）完全对得上，CSV+`history` 文件名为 v8.5 默认**。**待办**：本人覆盖 `cases\naca0012.cfg` 为手册修正版 → 重跑 `python tools/run_levels.py L1`。

### 09-10 进度更新（Arena 会话 01a085fd · S12′ L1 实测 + 勘误#9）

1. ✅ **L1 首跑跑通（09-09 夜本人跑）**：覆盖勘误#8 修正版 cfg 后 `python tools/run_levels.py L1` 成功——`history.csv` 1000 行、`Exit Success (SU2_CFD)`、残差 rms[Rho]→1e-2.76（1000 步预算内未达 1e-10，属预期，GCI 用一致步数预算）、`RefForce=45393.6` 正常（=q∞·S_ref）。
2. 🎯 **CD/CL 全 0 根因 + 勘误#9（09-09 夜排查）**：history.csv 的 `CD`/`CL`/`CF*` 列全为 0 → v8.5 非无量纲系数在 **`MARKER_MONITORING`** 标记面上积分（源码注释 + 官方 QuickStart `MARKER_MONITORING = ( airfoil )` 双证），手册 cfg 只有 `MARKER_EULER`。**手册已修**：cfg 加 `MARKER_MONITORING= ( Airfoil )`（42 行版）。**待办**：本人覆盖 cfg 重跑 L1 验 CD 非 0（量级 ~0.02）→ `python tools/run_levels.py L2 L3`。
3. 📐 **判据修正**：手册 S12′ 判据补「CD 末段稳定正数（0.01–0.03）、CL ~0.10–0.14」，并注明 S13′/S14′ 示例的 0.02342/0.02221/0.02158 是演示占位值（解析器自测用），绝对值以实测为准。
4. 🛠 **沙箱重建恢复（09-10 早，平台侧）**：沙箱工作区被平台重建为浅克隆（本地只见 7 文件、历史断在 `84dc29c`）；`git fetch --unshallow` 后确认**远端历史完好无损**（完整提交链 `84dc29c`→`560d8b1`→`562c729`→`f2d1274`→…→初始提交全部在），工作树复原干净。零内容损失，无需任何补救。
5. 🧪 **沙箱验证环境建成（09-10）**：SU2 v8.5.0 源码编译（meson、无 MPI/OMP、2 核、304 目标全过，与用户本机同版本同代码）；自建 O-grid `.su2` 生成器（gmsh 轮子的网格化核心在本沙箱环境损坏——4.11/4.14/4.15 四版本全灭、原因未明、**不影响用户侧**（其 gmsh CLI 09-09 实跑正常）；格式按 v8.5.0 reader 源码逐行定案，含两坑：名称行前 11 字符/计数行前 13 字符被 erase、须 `NDIME= 2`）。**新标准（本人指令 09-10）**：此后给本人的任何文件先沙箱实跑验证再发。
6. ✅ **42 行 cfg 实跑验证（09-10）**：真 SU2 v8.5.0 二进制 + 自制 O 网格（9600 节点/9360 四元）→ `Exit Success`、1000 步、`CD=0.0820`/`CL=0.0850` 非零且尾部稳定（rel_std 0.2%）、`RefForce=45393.6`（与用户机完全一致）、history.csv 1001 行/带引号表头/列名全对——**勘误#8/#9 全部键在 v8.5.0 实证有效**。CD 绝对值偏高为 O 网格粗糙工件（每边 120 点、无尾迹切割、一阶），非 cfg 问题，绝对值以用户 gmsh 网格实测为准。
7. ✅ **S13′ 解析器实跑验证 + 勘误#10（09-10）**：对真 history.csv 实跑抓出 `orders_drop=NaN`——真数据 rms 列**已是 log10 值**（如 -2.759，可为负），旧代码再取 log10；旧演示 fixture 是线性值（全正）把问题藏住了。修 `orders_dropped`（含负值→按 log10 列直接差值）+ 新增 log10 回归测试 `test_real_log10_history`；pytest 3/3 绿；修正版对真数据（1.947）与线性 fixture（4.95≈5.0）均正确。
8. ✅ **S12′ L1 实跑通过 + L2/L3 已跑完（09-10 本人跑）**：42 行版 cfg 生效——L1 1000 行、`Exit Success`、**CD=0.0286、CL=0.2455**（末段稳定；RefForce=45393.6 与沙箱一致）、L2/L3 跑完无报错。**权威锚点**：沙箱同版 SU2 v8.5.0 跑官方 QuickStart 算例（同翼型同工况、官方网格、二阶 JST、收敛到 1e-8）= **CL 0.3285 / CD 0.0215**；L1 一阶 1000 步值（CL 偏低 25%、CD 偏高 33%）方向正是一阶抹平加厚激波的预期（吸力峰抹平→升力降、波阻升），**物理自洽坐实**。CL 判据终改 **0.22–0.33**（此前 0.10–0.14 是不可压直觉、0.18–0.26 是线性理论估算，皆误；弱激波工况非线性升力远高于 PG 线性值 0.229）。CMx=0.1035 为 SU2 2D 标量力矩（∫(x·dFy−y·dFx)，命名习惯问题），T1 只用 DRAG，不影响主线。**（注：本条「L2/L3 已跑完无报错」仅指跑通；三档末段值回传后的预算问题见第 9 条。）**
9. 🔧 **S12′ 三档末段值回传 + 1000 步预算判定不足（09-10）**：tail50 均值——L1 CD=0.02905/CL=0.24123（在带内）、L2=0.04058/0.16518、L3=0.04771/0.10977（出带）。关键不是出带，是**趋势**：网格 20 万→77 万→300 万，CD 单调升、CL 单调降。一阶 ROE 是一致格式，细网格全收敛必趋近锚点解（CL≈0.33），L3 反向偏 2.6 倍在数学上不可能 → 诊断=**1000 步预算对细档不足**：cfg 是隐式欧拉+定 CFL=4，每步物理时间 ∝ Δx，L3 的 Δx=L1 的 1/4，同样 1000 步只覆盖 1/4 物理时间，激波/压力场没松弛完；L1 末段也在漂（999 步 CL=0.2456 vs 末 50 均值 0.2412），沙箱 ~1 万单元一阶跑 1000 步时末 500 步 CL 仍漂 ~10%，旁证预算普遍偏紧。**GCI（S14′）必须用三档同一收敛口径（力平台）的数据，1000 步数据不可用。**处置：① 本人跑三档漂移诊断一行命令（500–600 行 vs 末 50 行 + 末 100 行 min/max，cmd 一行版，沙箱已验）回传；② 沙箱 15 万单元 O 网格（≈L1 量级）同 cfg 跑 5000 步，取 1000 步点 vs 末段值校准**分档步数预算**（未验证前不发数）；③ 预算定稿后勘误#11（停止判据改「末 100 步 CD/CL 变化 <1%」，分档步数上限按物理时间 ∝ N·Δx 缩放）→ 重跑三档（挂机）→ S12′ 销项 → S13′/S14′。
10. 🔧 **漂移诊断回传坐实 + 沙箱 /tmp 被系统清理重建中（09-10）**：① 诊断输出（500–600 行→末 50 行）：L1 CD 0.0433→0.02905/CL 0.1526→0.24123、L2 0.05001→0.04058/0.12306→0.16518、L3 0.05575→0.04771/0.09103→0.10977——**三档全在同一轨迹上朝锚点（CD→0.0215/CL→0.3285）收敛**，网格越细每步物理时间越短（∝Δx）→ 同样 1000 步时越细的档走得越远不到终点；L1 末 100 行 CL 最大值=999 步值 → 末 100 步仍在 +8.5% 爬升（近平台未全停），CD 末 100 行 ±2.8% 带。② **base 环境误跑核实**：SU2 为独立 exe 不依赖 Python 环境，S12′ 数值有效（RefForce 与沙箱逐位一致），无需因环境重跑；手册 §0 补铁规说明；后续聊天指令一律带 `conda activate t1 &&`。③ **事故**：沙箱 /tmp 被系统清理（SU2 构建/网格/venv/校准跑全失）→ 已按 v8.5.0 源码网格 reader 定案重写 O 网格生成器（格式逐行对 CSU2ASCIIMeshReaderBase.cpp：NDIME/NPOIN/NELEM 各 erase(0,6)、marker 名行 erase(0,11)、计数行 erase(0,13)、标记部分=边界 LINE 元素定义非索引、0 起编号）；重建脚本（clone v8.5.0@12eb826 + eigen branches/3.3 + meson+ninja + 600 格冒烟 + 15 万格×5000 步校准）后台跑中，预计 2.5–3h。④ 分档步数预算（估算值，待校准后勘误#11 定稿）：750–1000 步每 25 步抽样命令已验证发出；若 L1 末段平 → L1 保留 1000 步数据、L2=4000、L3=8000；若 L1 仍爬 → L1=4000、L2=8000、L3=16000（L3 时间紧则 0.004 网格 r=1.5 + 8000 步备选）。

---

## 阶段 P0 · 拆雷（本周，总计 ≈1.7h）—— ✅ 全部结案（09-07，逐条见下）

### S01 · ~~Mr.GUO 整仓私有化（0.1h）~~ → 🟢 已撤销（09-07 保持公开；09-09 复查 public）
```bash
gh repo edit sunccchengze/Mr.GUO --visibility private --accept-visibility-change-consequences
```
- 连历史 blob 一起盖住，30 秒。**判据**：visibility 返回 `PRIVATE`。

### S01b · ~~公开白皮书仓~~ → 已取消（本人决定，2026-09-06）
- **决定**：白皮书为本人心血，保持私有（随私有仓 Mr.GUO 存放），不再建公开拆分仓。
- **战略收益**：弱点 No.5（弹药库公开）彻底闭合——T1–T8 选题与六软肋分析全部转入暗处，任何竞争对手无法再从公开渠道收割。
- **代价与对策**：白皮书价值唯一出口 = S19′ 信件的 PDF 附件（1:1 定向披露给郭老师）。该信分量因此加重，写作预算保持 0.75h 不变，冷置一夜纪律不变。
- **善后**：本地 `D:\guo-whitepaper` 文件夹删除；若已在 GitHub 建过空仓 guo-whitepaper，删除（网页 Settings → Delete this repository，或 `gh repo delete sunccchengze/guo-whitepaper --yes`）。

### S02 · ~~旗舰仓减重（0.5h）🤖~~ → ✅ 09-07 闭环（@f93801ab）
- `git rm -r --cached "技能库&准则"` + `.gitignore`（5 分钟）；NOTICES.md **让 AI 从目录名+许可证文件自动起草**，你只核对（15 分钟）；`git ls-tree main --name-only | grep -E '教材|\.pdf$'` 一条命令排查 `教材/`，命中则同批迁出。
- **判据**：main 文件数 <3,000；NOTICES.md 存在。

### S03 · ~~隐私收敛+门面（0.3h）~~ → ✅ 09-07 政策结案
- ~~wendang11~~（已私有，2026-09-06 验证）；剩余主目标：`SCZ_Archived`（含情绪/生日/私人单页，已核实仍公开）。命令清单照抄（`gh repo edit --visibility private`）；bio 三行 + pin 4 仓，网页点选 5 分钟。
- **判据**：陌生人 10 秒读出主线。

### S04 · ~~冻结令（0.25h）🤖~~ → ✅ 09-07 降级为监督协议（@38e1abb2）
- **决定已做出（2026-09-06）**：scz-Game-Studio 保留为「停机坪」——README 标注暂停营业与重启日期（2027 夏），冲刺期零投入；60 天内出现游戏代码提交则自动删除。AI 成文 `FREEZE-Mainline.md`，commit。价值在你的决定，不在文书。

---

## 阶段 A · 起步（第 1 周，总计 ≈3.5h）—— ✅ 全部结案（09-09：S05/S07/S08/S10′ ✅），交接阶段 B

### S05 · ~~delta-eta-min 建仓（0.5h）🤖~~ → ✅ 09-07 超额闭环（main=f0ae192）
- AI 一句话生成全套：目录骨架 + pytest 冒烟 + `AGENTS.md`（含铁律：**核心模块本人手写、AI 仅审查**）+ README（T1 定义引自白皮书 6.3）。你 review + push。
- **判据**：`pytest` 绿。

### S07 · ~~T1 证据源锁定（0.5h）🤖~~ → ✅ 09-09（本人报：SOURCES.md 数字→页码→用在哪逐行核完）
- AI 从白皮书自动摘 T1 相关键数字（P07 的 13±1 离散度与 +1.104%、P11 4200 s/样本、P16 正文/表格 10 倍差）成 `docs/SOURCES.md` 表：数字 → 页码 → 用在哪。你逐行核对出处。
- **判据**：T1 将引用的每个数字可回溯。

### S08 · ~~本地装 SU2 + 冒烟（1h）~~ → ✅ 09-09（随 §0 环境准备完成，本人报；SU2 v8.5.0 安装 + 冒烟横幅）
- **v2 简化：不租云**。装 SU2 走官方二进制（GitHub Releases，步骤见手册 §0）——pip/conda 无 SU2 求解器包（09-09 PyPI 实测 404，见审计纪要第 6 条）；官方最小算例（laminaire/圆柱）的收敛验证并入 S12′ 首次批量跑（首启即冒烟）。
- **判据**：本机 SU2 可跑且输出收敛史。

### S10′ · ~~🔒 一个 .geo 脚本出三档网格（1.5h）~~ → ✅ 09-09（本机三档跑通：L3=2,961,434 单元、markers=['Airfoil','Farfield']、L3 网格 102s；「SU2 各能启动 100 步」验并入 S12′ 首启）
- **v2 合并步（原 S06+S10+S11）**：拿 M6（或 0012）现成 `.geo` 模板，把特征长度提成**一个参数**，循环出 coarse/medium/fine 三档（细化比 ≥1.3）；拓扑检查（负体积/非流形）。三档参数表写进同一脚本的配置区——这就是原 S06 的"档位定义"，不需要独立模块。
- **判据**：三档网格生成成功、SU2 各能启动 100 步；脚本在手，档位可复现。
- **卡壳预案**：`.geo` 模板调不顺 → 直接用 SU2 官方不同细化度的现成网格（M6 有现成多网格），只要三档拓扑一致即可，GCI 不挑网格是谁生成的。

---

## 阶段 B · 心脏链（第 2–5 周，总计 ≈9.5h + 整夜挂机）

### S12′ · 三档一阶批量跑（1h 监控 + 挂机）🔧→✅ L1 实跑通过、L2/L3 已跑完（09-10：勘误#8/#9 全生效，L1 CD=0.0286/CL=0.2455；待三档末段值回传即销项）
- 2D 算例单档分钟级，本地连跑。批量脚本（🤖 可 AI 写，这不属于保护名单）+ `runs/README.md` 台账（档位/核时/残差终点）。

### S13′ · 🔒 收敛史解析器（2h）
- 手写 `src/convergence.py`：读 SU2 收敛史 → 残差/力目标序列 → 平台期自动判别。测试 fixture 直接用 S12′ 真日志。
- **范围纪律**：只支持这一种日志格式，不做通用性。**判据**：三档日志 → 统一摘要 JSON。

### S14′ · 🔒 GCI 模块（2h）
- 手写 `src/gci.py`：Richardson 外推 + GCI（Roache 公式，≈80 行）。用解析解自检用例验证收敛阶。
- **判据**：自检用例通过。

### S15′ · 🔒 Δη_min 检验（2.5h）★全计划心脏
- 手写 `src/significance.py`：网格噪声 + 迭代残差合成 Δη_min，**v1 只做 bootstrap 一种口径**；输出 `Δη > Δη_min?` 判据函数。合成数据（已知真值差）验证接受/拒绝正确。
- **判据**：测试绿，commit 无 co-author。

### S16′ · ⏳ 细档二阶深收敛（0.5h 监控 + 整夜挂机）
- 本地整夜跑细档二阶（残差 ≥4 个量级）。**跑成即在 LEDGER 标 🏁 第一个个人 E4。**（若本机内存不够再触发租云，预算仍 ≤¥300。）

### S17′ · 两张图（1h）🤖
- AI 按你的反 slop 规范出图：①三档 GCI 带；②"论文宣称增益 vs Δη_min 判据带"（P07 的 +1.104% 放进去）。你只核对数字与规范。

---

## 阶段 C · 外联与现场（并行固定槽，不可简化）

### S18 · 白板推导 ×12（45min/次）🔒
- **内容不砍，只降摩擦**：手机录像不剪辑；题序微调——前 4 次选你仓库里用过、最熟的东西（NSGA-II 非支配排序 / BatchNorm / MC Dropout / PointNet 对称函数），先赢习惯再上难度（Richardson 外推、bootstrap 与 S14′/S15′ 同周互证）。题目序列不变见 v1。
- **判据**：`drills/` 每次 1 mp4 + 1 照片。

### S19′ · 给郭老师第二封信（0.75h）
- **v2 简化治的是你的死磕**：300 字三段（一句进展附白皮书 PDF 附件 / 一个从 S12′–S14′ 真数据引出的问题 / 一个旁听组会请求）→ 30 分钟写完 → **冷置一夜，次晨只改错别字就发**。禁止第三轮打磨（风险警报 No.2 完美主义死磕）。

### S20 · 真人反馈 ×3（不变）
- 触发：S17′ 后、S24′ 后、组会旁听成行各一次。一封邮件同时带图+请求，别拆成两次打扰。

### S21′ · ~~赛事默认决策（0.25h）~~ → ✅ 09-07 默认美赛（FREEZE 附录 A）
- 两周内无报名截止 → **默认美赛（明年 2 月）**，写进 FREEZE 附录，不再议。

---

## 阶段 D · 交付（第 6–8 周，总计 ≈5.25h）

### S22′ · E4 报告（1h）🤖 — AI 从 LEDGER/runs 装配，你核每个数字可回溯。
### S23′ · 线上站新页（0.75h）🤖 【🔶 半步 ✅：占位骨架已上 arena 分支，数据待 S17′】 — 你的 AI 舰队主场，全包：E4 卡片+两图挂 methodology 页，部署绿即收。
### S24′ · T1 报告 v1（1.5h）🤖 — 你先手写 5 行骨架（动机/方法/实证/回判表/局限），AI 装配成文，你逐数字对 SOURCES.md。自检三问：训练集还是测试集？怎么复现？哪个数字没出处？
### S25 · 师兄评审（1h）— 要 3 条狠批，落实或反驳进 `docs/review_r1.md`。
### S26 · 60 天对表（1h）🔒 — 2026-11-06，LEDGER 逐行对 git，诚实写 `docs/AUDIT-1106.md`，未完成项不许擦除。

---

## 依赖关系（v2）

```
S01(30秒私有化) → S01b(已取消·白皮书保持私有) → S02 → S03 → S04   （P0，一周内 ≈1.7h）
S05 → S07 → S08 → S10′ → S12′                   （起步链，第 1–2 周；S05/S07 与 S08 可并行）
S12′ → S13′ → S14′ → S15′ → S16′ → S17′         （心脏链，第 2–5 周，🔒 密集区）
S18 每周固定槽；S19′ 最早在 S12′ 有真数据后发
S17′ → S22′/S23′/S24′ → S25 → S26               （交付链，第 6–8 周）
```

## Phase 2（60 天检查点全绿后才解锁，现在不做）
- 算例升级：M6 → 2D 跨声速叶栅（复用 S10′ 脚本换几何）→ 视情况 3D + 租云；
- t 检验第二口径、T1 报告投预印本（先过师兄关）；
- 对 20 篇论文的完整回判表（v1 只回判 P07/P16 两篇）。

## 一句话版本

> **砍掉一切"只要拥有"的步骤的时间，保护一切"必须亲手做"的步骤的完整。** 21 小时核心工时 + 每周 45 分钟白板 + 三封真的信，60 天后你手里是：一个手写的统计框架、一条真的二阶收敛曲线、一条真的导师对话线——这三样，任何竞争对手的 AI 舰队都替他造不出来。
